import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'JSA System - Offshore Safety Analysis',
  description: 'AI-Powered Job Safety Analysis System for Offshore Accommodation Work Barges',
  keywords: ['JSA', 'Job Safety Analysis', 'Offshore Safety', 'Risk Assessment', 'Chevron Energy Wheel'],
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <div className="min-h-screen bg-gray-50">
          {children}
        </div>
      </body>
    </html>
  );
}
